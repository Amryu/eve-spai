# UI-053 &mdash; The in-app map menu still has its old entries

| | |
|---|---|
| **Severity** | Medium |
| **Status** | Open |
| **Region** | `app.rs` map context menu |
| **Reported by** | user |

## Ask

"Clean up the in app map context menu to be the same as the web one. Only keep the 'Favourite' option
(in both places)." &mdash; followed later by "The in-app context menu has not been adapted to match the
web version yet?"

## Gap

Two dozen entries across four mode-dependent blocks, most of them a second way to do something the
route drag now does directly. A menu of second ways is how nobody finds the first one.
