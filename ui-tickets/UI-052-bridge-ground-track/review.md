# UI-052 review cycle

**Status:** Fixed
**Branch:** `web/web-028-layout-and-jabber`, shared

`arch_ground` is gone, call and function. The arc carries the meaning on its own; the dashed track
under it competed with the two dashed conventions the same map already uses for regional and
inter-constellation gates.

The web map never had one, so the two maps now agree.
